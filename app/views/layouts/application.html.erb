<!DOCTYPE html>
<html>
  <head>
    <title>Cardreader</title>
    

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
	<%= csrf_meta_tags %>
  </head>

  <body>
	<div class="header">
		<div class="container">
			<div class="nav pull-right">
			<div class="dropdown">
				
				<% if current_user %> 
				<button class="btn btn-account dropdown-toggle" type="button" data-toggle="dropdown"> <%= current_user.email %> <span class="caret"></span></button>
					<ul class="dropdown-menu"> 	
						<li><%= link_to "Log out", logout_path, method: "delete" %></li> 
					</ul> 
				<% else %> 
				<button class="btn btn-account dropdown-toggle" type="button" data-toggle="dropdown">Account <span class="caret"></span></button>
					<ul class="dropdown-menu"> 
						<li><%= link_to "Login", 'login' %></a></li> 
						<li><%= link_to "Signup", 'signup' %></a></li> 
					</ul> 
				<% end %>
			<div>
			</div>
		</div>
	</div>
    <%= yield %>
  </body>
</html>
